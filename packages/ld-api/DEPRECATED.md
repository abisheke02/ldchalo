# DEPRECATED — ld-api

This package has been superseded by `packages/api` (the unified API).

**All routes and services have been migrated to:**
- Routes → `packages/api/src/modules/ld/`
- Services → `packages/api/src/services/`
- Config → `packages/api/src/config/`

**Do not add new code here.** This directory is kept temporarily for reference only and will be removed in the next cleanup pass.

Migration completed: 2026-05-27
