FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install          # full install including devDependencies (nodemon)

COPY . .

EXPOSE 3000

# nodemon restarts on file changes (volume-mounted from host)
CMD ["npx", "nodemon", "--watch", "src", "src/index.js"]
