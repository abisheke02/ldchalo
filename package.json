{
  "name": "LD Schools",
  "version": "1.0.0",
  "description": "LD Schools — Learning Disability & School Management Platform",
  "private": true,
  "scripts": {
    "dev":          "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix web\"",
    "dev:backend":  "npm run dev --prefix backend",
    "dev:web":      "npm run dev --prefix web",
    "build:web":    "npm run build --prefix web",
    "build:all":    "npm run build --prefix web && npm run build:renderer --prefix desktop",
    "docker:up":    "docker compose up -d --build",
    "docker:down":  "docker compose down",
    "docker:logs":  "docker compose logs -f api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm":  ">=10.0.0"
  }
}

